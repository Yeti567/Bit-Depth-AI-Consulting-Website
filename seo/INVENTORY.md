# SEO Inventory (Phase 0)

Generated 2026-05-06. No edits made in this phase. This is the source-of-truth read of the site state before Phase 1.

---

## 1. Page-by-page metadata inventory

| Route | File | Title (truncated) | Description (truncated) | Canonical | Schema | Words | In sitemap |
|---|---|---|---|:-:|---|:-:|:-:|
| `/` | `src/app/page.tsx` | AI and Automation for Canadian Trades and Contractors \| BitDepth AI | Vendor-neutral AI consulting for Canadian trades... Audits from $5,000. | self | Organization, LocalBusiness, WebSite, FAQPage | 2000+ | Y |
| `/about` | `src/app/about/page.tsx` | About BitDepth AI Consulting \| Blake Cowan | BitDepth exists because Canadian trades and small contractors are one of... | self | none | 500-1000 | Y |
| `/contact` | `src/app/contact/page.tsx` | Contact \| BitDepth AI Consulting | Tell us what is breaking. The fastest way to get help is to book an audit. | self | none | 200-500 | Y |
| `/services` | `src/app/services/page.tsx` | AI Consulting Services for Canadian Trades \| BitDepth AI | Five AI consulting services... Audit, Missed Call Text Back, Implementation. | self | none | 500-1000 | Y |
| `/services/ai-audit` | `src/app/services/ai-audit/page.tsx` | AI Opportunity Audit for Canadian Trades \| BitDepth AI | A structured 14 day diagnostic of your trades or contractor business. $5,000 flat. | self | Service | 500-1000 | Y |
| `/services/ai-implementation` | `src/app/services/ai-implementation/page.tsx` | AI Implementation for Canadian Trades \| BitDepth AI | Phased implementation of AI workers, integrations, dashboards... | self | Service | 500-1000 | Y |
| `/services/custom-ai-workers` | `src/app/services/custom-ai-workers/page.tsx` | Custom AI Workers for Trades and Contractors \| BitDepth AI | Purpose-built AI agents... From $10,000. | self | Service | 500-1000 | Y |
| `/services/missed-call-text-back` | `src/app/services/missed-call-text-back/page.tsx` | Missed Call Text Back for Canadian Trades \| BitDepth AI Consulting | Recover the calls you already paid to attract... From $497 per month. | self | Service, FAQPage | 2000+ | Y |
| `/services/workflow-automation` | `src/app/services/workflow-automation/page.tsx` | Workflow Automation for Canadian Trades \| BitDepth AI | Stop your office from being the integration... From $5,000 per workflow. | self | Service | 500-1000 | Y |
| `/ai-audit` | `src/app/ai-audit/page.tsx` | AI Opportunity Audit for Canadian Trades \| BitDepth AI | Find out exactly where AI will save your business... Audits from $1,500. | self | FAQPage (audit) | 1000-2000 | Y |
| `/ai-booking-agent` | `src/app/ai-booking-agent/page.tsx` | AI Booking Agent for Trades and Service Businesses \| BitDepth AI Consulting | Stop losing jobs when you miss a call... For HVAC, plumbing, electrical, roofing... | self | Service | 1000-2000 | Y |
| `/profit-leak-review` | `src/app/profit-leak-review/page.tsx` | Profit Leak Detection Review \| BitDepth AI Consulting | For $250, we review where your business is losing revenue... | self | Service | 1000-2000 | Y |
| `/security-and-data` | `src/app/security-and-data/page.tsx` | Security and Data Handling \| BitDepth AI Consulting | Plain-English answers on how we handle your data... | self | none | 1000-2000 | Y |
| `/missed-call-revenue-recovery` | `src/app/missed-call-revenue-recovery/page.tsx` | Missed Call Revenue Recovery for Canadian Trades \| BitDepth AI | Calculate how much revenue your HVAC, plumbing, electrical... is losing. | self | FAQPage | 2000+ | **N** |
| `/industries` | `src/app/industries/page.tsx` | AI Consulting by Industry: Trades, ICI, Field Services \| BitDepth AI | AI consulting built for Canadian field service businesses... | self | none | 500-1000 | Y |
| `/industries/cor-pathway` | `src/app/industries/cor-pathway/page.tsx` | COR Certification for Ontario Contractors \| BitDepth AI | COR Pathway, BitDepth sister company... | self | none | 500-1000 | Y |
| `/industries/electrical` | `src/app/industries/electrical/page.tsx` | AI for Ontario Electrical Contractors \| BitDepth AI | ESA notification automation, permit tracking, apprentice hour capture... | self | none | 500-1000 | Y |
| `/industries/field-services` | `src/app/industries/field-services/page.tsx` | AI for Field Service Businesses in Canada \| BitDepth AI | Renewal automation, route optimization... pest control, restoration, cleaning. | self | none | 500-1000 | Y |
| `/industries/hvac-plumbing` | `src/app/industries/hvac-plumbing/page.tsx` | AI for HVAC and Plumbing Contractors \| BitDepth AI | Stop losing 30 to 50 percent of your inbound calls... | self | none | 500-1000 | Y |
| `/industries/professional-services` | `src/app/industries/professional-services/page.tsx` | AI for Law Firms, Accountants, and Brokerages \| BitDepth AI | Private intake automation, document drafting from precedents... | self | none | 500-1000 | Y |
| `/industries/roofing-ici-construction` | `src/app/industries/roofing-ici-construction/page.tsx` | AI for Roofing and ICI Construction in Ontario \| BitDepth AI | Compliance documentation, sub-contractor pre-qualification... AuditSoft. | self | none | 500-1000 | Y |
| `/resources` | `src/app/resources/page.tsx` | AI Resources for Canadian Trades and Contractors \| BitDepth AI | Plain-language guides, downloads, and case studies on AI for Canadian trades. | self | none | 500-1000 | Y |
| `/resources/[slug]` | `src/app/resources/[slug]/page.tsx` | (per-article via `generateMetadata`) | (per-article) | self | Article (varies) | varies | Y (filtered) |
| `/resources/five-signs-your-business-is-ready-for-ai` | (standalone) | Five Signs Your Business Is Ready for AI \| BitDepth AI | A practical checklist for Canadian business owners evaluating AI readiness. | self | Article | 1000-2000 | Y |
| `/resources/what-can-i-expect-in-an-ai-discovery-audit` | (standalone) | What Can I Expect in an AI Discovery Audit? \| BitDepth AI | Wondering what an AI discovery audit involves? Step-by-step process. | self | Article | 1000-2000 | Y |
| `/resources/what-to-expect-from-your-first-ai-consulting-engagement` | (standalone) | What to Expect From Your First AI Consulting Engagement \| BitDepth AI | A step-by-step walkthrough of the BitDepth process. | self | Article | 1000-2000 | Y |
| `/resources/you-dont-have-an-ai-problem` | (standalone) | You Don't Have an AI Problem \| BitDepth AI | (per article) | self | Article | 1000-2000 | Y |
| `/privacy-policy` | `src/app/privacy-policy/page.tsx` | Privacy Policy \| BitDepth AI Consulting | Review the BitDepth AI Consulting privacy policy. | self | none | 500-1000 | Y |
| `/terms-of-use` | `src/app/terms-of-use/page.tsx` | Terms of Use \| BitDepth AI Consulting | Review the standard terms that govern use of the BitDepth AI Consulting website. | self | none | 200-500 | Y |

### Active dynamic resource slugs (in `generateStaticParams`)
- `is-your-data-ready-for-ai`
- `what-is-an-ai-discovery-audit`

### Retired dynamic resource slugs (filtered out in both `generateStaticParams` and sitemap)
- `dead-in-the-field-why-predictive-maintenance-cant-wait`
- `digital-twin-technology-how-smart-manufacturers-are-pulling-ahead`
- `see-every-defect-ai-computer-vision-quality-control`
- `how-to-evaluate-ai-use-cases-in-industrial-operations`
- `what-vendor-neutral-ai-consulting-actually-means`
- `building-an-ai-roadmap-for-canadian-mid-market-companies`

These slugs still exist as data inside `src/app/resources/[slug]/page.tsx` (about 500 lines of legacy oil-and-gas / manufacturing / predictive-maintenance content) but they do not render and are not sitemapped. They also have explicit 308 redirects in `next.config.mjs` pointing to `/resources`.

---

## 2. Pages NOT in `src/app/sitemap.ts`

- `/missed-call-revenue-recovery` — full landing page, indexable, has FAQ schema, but is omitted from the dynamic sitemap. **Either include it or 308 redirect it to `/services/missed-call-text-back`.**

All other tracked routes are listed.

---

## 3. Pages with `noindex`

None found. Every page returns `index: true, follow: true` (default) or sets `robots` explicitly to true. Privacy and Terms both explicitly index.

---

## 4. Pages with weak / missing metadata

- **No critical gaps.** Every static page has both `title` and `description`. Every page has a self-referencing canonical via `alternates.canonical`. The standalone resource articles set their own canonicals. The dynamic `/resources/[slug]` route sets canonical via `generateMetadata`.

### Pages with **no JSON-LD schema** at all
- `/about`
- `/contact`
- `/services` (root)
- `/security-and-data`
- `/industries` (root)
- `/industries/cor-pathway`
- `/industries/electrical`
- `/industries/field-services`
- `/industries/hvac-plumbing`
- `/industries/professional-services`
- `/industries/roofing-ici-construction`
- `/resources` (root)
- `/privacy-policy`
- `/terms-of-use`

### Schema gaps to address in Phase 2
- No `BreadcrumbList` JSON-LD on any page (the `buildBreadcrumbSchema` helper exists in `src/lib/schema.ts` but is unused).
- No `Article` schema on the standalone resource pages (only on dynamic `[slug]` route, but the four standalone articles also use `buildArticleSchema` actually — verified). _Update: Article schema IS present on all four standalone articles plus the dynamic route. Inventory column corrected above._
- Industry pages have no `Service` or `FAQPage` schema even when they have FAQ-like content.
- No `Organization` / `LocalBusiness` schema outside the homepage. Per the audit, this is fine on a single-page (the org is global), but a `WebSite`/`Organization` injection in `app/layout.tsx` would propagate to every page.

---

## 5. Sitemap, robots, redirects

### Sitemap conflict
- **`src/app/sitemap.ts`** (dynamic, current, 23 static URLs + dynamic resource slugs)
- **`public/sitemap.xml`** (static, 189 lines, dated 2026-04-05, contents drift from the live route map)

Both serve at `/sitemap.xml`. In Next.js App Router, the dynamic route from `app/sitemap.ts` should win, but the static file in `/public` is a footgun. **Recommend deleting `public/sitemap.xml` in Phase 2.**

### Static URLs currently in `src/app/sitemap.ts`
`/`, `/ai-booking-agent`, `/profit-leak-review`, `/ai-audit`, `/security-and-data`, `/services`, `/services/ai-audit`, `/services/missed-call-text-back`, `/services/ai-implementation`, `/services/workflow-automation`, `/services/custom-ai-workers`, `/industries`, `/industries/hvac-plumbing`, `/industries/electrical`, `/industries/roofing-ici-construction`, `/industries/field-services`, `/industries/professional-services`, `/industries/cor-pathway`, `/about`, `/resources`, `/contact`, `/privacy-policy`, `/terms-of-use`.

**Missing from sitemap.ts:**
- `/missed-call-revenue-recovery` (live route, 200 OK, indexable)
- The four standalone resource articles (they live at `/resources/[slug]`-style URLs but as separate files, not via `generateStaticParams`). Need to verify whether the dynamic resource sitemap entries actually include the four standalone articles' slugs.

_(Verification flag: the dynamic resource entries are derived from `Object.keys(resourceArticles)` in `src/app/resources/[slug]/page.tsx`, which only contains the dynamic route's articles. The four standalone article folders are separate files and would NOT be added by that loop. Phase 2 should explicitly add them.)_

### `public/robots.txt` (current)
```
User-agent: *
Allow: /
Disallow: /api/

User-agent: ChatGPT-User    Allow: /
User-agent: GPTBot          Allow: /
User-agent: anthropic-ai    Allow: /
User-agent: PerplexityBot   Allow: /
User-agent: CCBot           Allow: /

Sitemap: https://bitdepthaiconsulting.com/sitemap.xml
```

No `app/robots.ts` exists. If Phase 2 adds `app/robots.ts`, the static `public/robots.txt` should be deleted (same conflict pattern as the sitemap).

### Redirects in `next.config.mjs` (all permanent / 308)
```
/ai-readiness-assessment              → /ai-audit
/ai-assessment                        → /ai-audit
/ai-strategy                          → /services/ai-implementation
/services/ai-strategy                 → /services/ai-implementation
/services/ai-training                 → /services
/services/ai-workers                  → /services/custom-ai-workers
/industries/oil-and-gas               → /industries
/industries/manufacturing             → /industries
/industries/transportation            → /industries
/industries/construction              → /industries/roofing-ici-construction
/resources/dead-in-the-field-...      → /resources
/resources/digital-twin-technology... → /resources
/resources/see-every-defect-...       → /resources
/resources/how-to-evaluate-ai-...     → /resources
/resources/what-vendor-neutral-...    → /resources
/resources/building-an-ai-roadmap-... → /resources
```

---

## 6. Stale-positioning grep results

### `oil and gas` / `oil & gas` / `oilfield` (case-insensitive)
- **1 file:** `src/app/resources/[slug]/page.tsx` — all hits inside the retired article data (`dead-in-the-field-...`, `digital-twin-...`, `see-every-defect-...`, `how-to-evaluate-...`). These articles are filtered out of `generateStaticParams` and the sitemap, and have explicit 308 redirects. **Source data still present, but nothing renders.**

### `manufacturing` / `predictive maintenance` / `enterprise AI` / `mid-market` (case-insensitive)
- **2 files:**
  - `src/app/sitemap.ts` line 17 — only inside the `retiredResourceSlugs` filter (legitimate).
  - `src/app/resources/[slug]/page.tsx` — all hits inside retired article data, plus one stray on line 57 inside an active article (`what-vendor-neutral-...` which is itself in the retired set; double-check this is not in an active article).

**Decision needed for Phase 1:** strip the retired-article data from `src/app/resources/[slug]/page.tsx` to reduce file size and eliminate any chance of accidental indexing. The redirects already handle the URLs; the data is dead weight.

### Other live-page mentions
- No `oil and gas` / `oilfield` / `predictive maintenance` / `enterprise AI` text on any rendered live page.
- No `manufacturing` text on any rendered live page (homepage, services, industries pages all use trades/contractor language now).
- No `mid-market` text on any rendered live page.

This is a strong result. The audit's concern was about Google's cached snippets and external bios still showing oil-and-gas language. The live pages are clean. **Phase 1 should focus on requesting reindex in GSC and updating off-site bios, not on rewriting live pages.**

---

## 7. Conflicts between this prompt and recent commits

The prompt directs several changes that flip decisions made in the last three commits. Surfacing these now so Phase 1 can be approved with eyes open.

### Conflict A: Canonical AI audit page

| Source | Decision |
|---|---|
| Prompt (this task) | Keep `/services/ai-audit` at flat $5,000. Retire `/ai-audit` (308 → `/services/ai-audit`). |
| Last commit (`32c201f`) | Made `/ai-audit` the canonical "deeper strategic step" page. Updated nav and footer to point "AI Opportunity Audit" → `/ai-audit`. |
| Audit (source) | Recommends consolidation either direction; says delete `/ai-audit` and redirect to `/services/ai-audit` IF the $5,000 flat is the real offer. |

The prompt's instruction is consistent with the audit's "if flat $5,000 is real" branch and with project memory (`Audit Credit Guarantee, $5,000, 90 days`). Following the prompt requires reverting the recent nav edits and the hero-softening I added to `/ai-audit` last task.

### Conflict B: URL location of two new pages

| Source | Decision |
|---|---|
| Prompt | Move `/ai-booking-agent` → `/services/ai-booking-agent` and `/profit-leak-review` → `/services/profit-leak-review`. |
| Last commit | Built both at root paths (`/ai-booking-agent`, `/profit-leak-review`). |
| Audit | "Not fatal," but recommends the `/services/...` structure as cleaner IA. |

Following the prompt requires moving both files, adding redirects from the old root paths, and updating every internal link (homepage, footer, nav, AI Audit page CTAs, the booking-agent's own internal references, etc.).

### Conflict C: AI Booking Agent pricing

| Source | Pricing |
|---|---|
| Prompt's offer matrix | "From $797 per month" |
| Page I built last commit | "$600 setup, $250/mo (chatbot starter)" + "$1,500 setup, varies monthly (phone agent)" |

These are different products at different price points. The prompt's $797/mo is a single flat-monthly; the live page is a two-tier offer. Following the prompt requires deciding whether to (a) keep the two-tier structure and adjust `lib/offers.ts` to fit, or (b) collapse to a single $797/mo tier. **Needs Blake's call.**

### Conflict D: Audit recommendation references

The audit cites the homepage as showing "Starting at $5,000" alongside a $250 Profit Leak Review and a tiered `/ai-audit`. The current homepage hero (after last commit) reads "From $497 per month" and does not surface the $5,000 audit at all in the price anchor. So the snapshot Google indexed is older than what is now live. Phase 1 should still execute the offer-matrix consolidation, but the homepage is closer to the new state than the audit suggests.

### Conflict E: Checklist count

The audit flags `20-point checklist` vs `30-point checklist`. The prompt says default to 30. Need to grep the codebase in Phase 1 for both strings and reconcile.

```
grep -rn "20-point\|30-point\|20 point\|30 point" src/
```

(Not run yet — Phase 0 is inventory only.)

---

## 8. Open questions for Blake before Phase 1

1. **Canonical audit URL.** Confirm the prompt's direction: keep `/services/ai-audit` at flat $5,000, retire `/ai-audit`. (If yes, this reverses last commit.)
2. **`/ai-booking-agent` and `/profit-leak-review` URL location.** Move both under `/services/`? (If yes, this is a multi-page move with redirects + every internal-link sweep.)
3. **AI Booking Agent pricing.** Single $797/mo flat vs. the two-tier $600 setup / $250 mo (chatbot) and $1,500 setup / variable (phone) the page currently has?
4. **Phone number for NAP block (Phase 4).** Real number or `// TODO` placeholder?
5. **GBP categories and confirmation.** Phase 4 builds 5 location pages. Confirm the cities (Toronto, GTA umbrella, Hamilton, Kitchener-Waterloo, Calgary) match Blake's actual service-area priority.
6. **Retired article data.** OK to delete the ~500 lines of oil/gas/manufacturing article data from `src/app/resources/[slug]/page.tsx` in Phase 1? Redirects already handle the URLs.

---

## 9. Phase 0 status

- Inventory complete.
- No edits made.
- Six open questions above.
- Three recent-commit conflicts with the prompt's directives.
- Live pages are clean of stale positioning. Off-site bios and Google's cache are the only stale-signal surfaces, and those are off-codebase work.

**Stopping here per the prompt's "do not start the next phase until the current phase report is complete" rule.**
